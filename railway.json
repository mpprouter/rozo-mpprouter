{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "scripts/e2e/Dockerfile" },
  "deploy": { "cronSchedule": "0 1 * * 1,4", "restartPolicyType": "NEVER" }
}
