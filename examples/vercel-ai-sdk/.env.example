# Stellar mainnet secret key of the wallet that pays. Never commit a real one.
# The account needs a USDC trustline, a small USDC balance, and >1 XLM reserve.
STELLAR_SECRET=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Only needed for `npm run demo:agent`.
OPENAI_API_KEY=sk-...

# Optional overrides.
# MPP_ROUTER_URL=https://apiserver.mpprouter.dev
# STELLAR_RPC_URL=https://mainnet.sorobanrpc.com
